# for clapboard

launcher = [ "tofi", "--fuzzy-match=true", "--prompt-text=Clipboard 󰈿" ]
history_size = 50

[favorites]
# You can add your favorite clipboard pastes here like this:
# "some key" = "some value"
