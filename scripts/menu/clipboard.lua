-- Clipboard history menu
-- TODO make sure that the clapboard config toml file has the current tofi theme somehow

-- local home = os.getenv("HOME")
-- local S = dofile(home .. "/.aquamoon/scripts/sys/settings.lua")

os.execute("clapboard")
